<%= form_for [user, item], remote: true do |f| %>
<div class="input-group">
  <%= f.text_field :name, placeholder: "Add a new to do item", class: "form-control form-control-lg"%>
  <span class="input-group-btn">
    <%= f.submit "DoIt!", class: "btn btn-success" %>
  </span>
</div>
<% end %>
